Read `SPEC.md`, `WHY.md`, the per-feature specs, and scan the current codebase. Generate a ~10-minute judge demo script.

Structure:
1. Open with the 10x ambition — a live prediction product for a tournament happening now (June 2026).
2. Make a live prediction on a real seeded match (the spine: predict).
3. Admin confirms an official result → show base points calculated deterministically (settle → score).
4. Show the leaderboard updating with the tiebreak (exact-score accuracy, then earliest registration).
5. Show cancel/postpone or audited re-settlement as the "correctness under change" moment.
6. Close on verification rigor: a holdout scenario passing (order-independence / points-conservation).

For each step: what to click, what to say, what to highlight. Add 3 likely judge questions with strong answers (lean on WHY.md). Keep claims aligned with the MVP scope (base scoring; bonuses/groups/real-time are tier 2).
